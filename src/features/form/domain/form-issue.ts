/**
 * Shared diagnostic contract used to report schema and business-rule failures
 * consistently across import, export, persistence, and user-facing messages.
 */
export type { FormIssue } from "jb-form-builder/contract";
